#include <stdlib.h>
#include <time.h>
/**
*main - Entry point
*Description: 'random positive negative integer'
*Return: always 0
*/


/* betty style doc for function main goes there */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	 
	return (0);
}
